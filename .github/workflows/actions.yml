on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  run-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Node
        uses: actions/setup-node@v2
        with:
          node-version: 18.x
      - name: node ci, build, and test
        run: |
          npm --prefix src/front ci
          npm --prefix src/front run build --if-present
          npm --prefix src/front test
